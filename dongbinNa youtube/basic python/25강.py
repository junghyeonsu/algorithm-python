a = [10, 20, 30, 40, 50]
b = [70, 50, 40]
# print(a.count(10)) # 원소가 몇 개 포함되어 있는지 반환

# print(a.index(50)) # 원소가 몇 번째 인덱스에 있는지 반환

# a.append(25) # 리스트의 맨 마지막에 새로운 원소를 삽입

# 정렬 함수
# a.sort() # 오름차순 정렬이 default

# a.extend(b) # a에 뒤쪽에 b 리스틀 합친다.
# print(a) #  [10, 20, 30, 40, 50, 70, 50, 40]

# a.insert(3, 70) # 특정한 위치에 특정한 원소를 넣기
# a.remove(10) # 특정한 원소 삭제 (앞에서 부터 확인해서 앞에서부터 삭제함)
# a.pop(3) # 특정 인덱스의 원소를 삭제
# a.reverse() # 리스트 순서 뒤집기
